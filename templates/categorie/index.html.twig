{% extends 'base.html.twig' %}

{% block body %}
    <h1>La liste des catégories</h1>
{% for categorie in categories %}
    <p><a href="{{ path('show_categorie', {'id': categorie.id}) }}">{{ categorie.nom_categorie }}</a></p><br>
{% endfor %}

{% endblock %}

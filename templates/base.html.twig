<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {# {{ encore_entry_link_tags('app') }} #}
        {% endblock %}

        {% block javascripts %}
            {# {{ encore_entry_script_tags('app') }} #}
        {% endblock %}
    </head>
    <body>

{% if app.user %}
    <a href="{{ path('app_logout') }}">Se déconnecter</a>
    <a href=""{{ app.user.pseudo }}></a>
{% else %}
    <a href="{{ path('app_login') }}">Se déconnecter</a>
    <a href="{{ path('app_register') }}">S'enregistrer</a>
{% endif %}

<nav>
    <ul>
        {# <li><a href="{{ path('app_home') }}">Home</a></li> 
        <li><a href="{{ path('app_plan') }}">Plan</a></li>
        <li><a href="{{ path('app_horaires') }}">Horaires</a></li> #}
        <li><a href="{{ path('app_categorie') }}">Forum</a></li>
        {# <li><a href="{{ path('app_alerte') }}">Alerte</a></li> #}
    </ul>
</nav>

        {% block body %}{% endblock %}
    </body>
</html>
